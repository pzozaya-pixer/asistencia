# apps/api

API `NestJS` base para la demo con:

- `health`
- `auth`
- `activities`
- `attendees`
- `attendance`

Todo usa servicios `mock` mientras cerramos la decisión entre `Supabase Auth` y `JWT propio`.
