# apps/web

Frontend `Next.js` para la demo PWA. Incluye una superficie única con:

- acceso responsable
- resumen administrativo
- flujo asistente con QR temporal visual
- vista responsable de validación y firma

Los datos actuales son `mock` y viven en `lib/mock-data.ts`.

